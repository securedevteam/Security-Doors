@model RoleViewModel

@{
    ViewData["Title"] = "Смена ролей для пользователя - Security Doors";
}

<div class="text-center">
    <h2>Изменение ролей для пользователя @Model.UserEmail</h2>
</div>

<form asp-action="EditUsersRoles" method="post">
    <input type="hidden" name="userId" value="@Model.UserId" />

    <div class="d-block my-3">
        @foreach (IdentityRole role in Model.AllRoles)
        {
            <input type="checkbox" name="roles" value="@role.Name"
                   @(Model.UserRoles.Contains(role.Name) ? "checked=\"checked\"" : "") />@role.Name <br />
            }
    </div>

    <button class="btn btn-primary btn-lg btn-block" type="submit">Сохранить изменения</button>
</form>
